{
  "name": "gamepals",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @gamepals/client dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=22"
  }
}
